class Solution {
    public int computeGCD(int a, int b) {
        // code here
        int res=1;
        int min=Math.min(a,b);
        for(int i=2;i<=min;i++)
        {
            if(a%i==0 && b%i==0)
            res=i;
        }
        return res;
    }
}
