class Solution {
    static boolean armstrongNumber(int n) {
        // code here
        int res=0;
        int num=n;
        while (n>0)
        {
            int dig=n%10;
            res=res+dig*dig*dig;
            n/=10;
        }
        return (num==res);
    }
}
