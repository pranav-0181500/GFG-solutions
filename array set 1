class Solution {
    public String average(int arr[]) {
        // code here
        double sum=0;
        double ans=0;
        for (int i=0;i<arr.length;i++)
        {
            sum+=arr[i];
            
        }
          ans=sum/arr.length;
            
            return String.format ("%.2f",ans);
    }
}
