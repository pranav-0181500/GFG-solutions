class Solution {
    public int reverseDigits(int n) {
        int r=0;
        while(n!=0)
        {
            int dig=n%10;
            r=r*10+dig;
            n=n/10;
        }
        return r;
    }
}
