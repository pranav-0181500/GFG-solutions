class Solution {
    public void reverseArray(int arr[]) {
        // code here
        int i=0;
        int n=arr.length-1;
        while (i<=n)
        {
            int temp=arr[i];
            arr[i]=arr[n];
            arr[n]=temp;
            i++;
            n--;
        }
    }
}
