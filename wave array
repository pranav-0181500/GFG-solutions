class Solution {
    public void sortInWave(int arr[]) {
        // code here
        int i=0;
        int n=arr.length-1;
        while (i<n)
        {
            int temp=arr[i];
            arr[i]=arr[i+1];
            arr[i+1]=temp;
            i=i+2;
        }
    }
}
