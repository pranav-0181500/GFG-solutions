class Solution {
    static List<Integer> get(int a, int b) {
        a=a^b;
        b=a^b;
        a=a^b;
        ArrayList<Integer> l1=new ArrayList<>();
        l1.add(a);
        l1.add(b);
        return l1;
    }
}
